{
  "name": "locale-region-list",
  "version": "0.0.1",
  "main": "index.js",
  "author": "boris huezo",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "semantic-release-plus --extends ./config/release.config.js"
  },
  "keywords": [
    "locale",
    "region",
    "list"
  ],
  "repository": {
    "type": "git",
    "url": "git@github.com:bhuezodd/locale-region-list.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "tag": "latest"
  },
  "dependencies": {
    "semantic-release-plus": "^20.0.0"
  },
  "devDependencies": {
    "@semantic-release/changelog": "^6.0.1",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^8.0.4"
  }
}
